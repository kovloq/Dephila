<%= form_with(model: administrator_news, local: true) do |form| %>
  <% if administrator_news.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(administrator_news.errors.count, "error") %> prohibited this administrator_news from being saved:</h2>

      <ul>
      <% administrator_news.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
