<%= form_with(model: administrator_hotel, local: true) do |form| %>
  <% if administrator_hotel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(administrator_hotel.errors.count, "error") %> prohibited this administrator_hotel from being saved:</h2>

      <ul>
      <% administrator_hotel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
